apiVersion: v1
kind: Service
metadata:
  name: mi-api
  namespace: docker-testing
  labels:
    app: mi-api
spec:
  type: ClusterIP
  selector:
    app: mi-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
